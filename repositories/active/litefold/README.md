# LiteFold 外层目录说明

`repositories/active/litefold/` 是 LiteFold 在主工作区中的外层挂载目录。

## 当前结构

- `source/`：真实的 LiteFold 上游仓库工作树

## 说明

主仓库当前记录的 gitlink 路径是：

- `repositories/active/litefold/source`

因此：

- 如果你只是浏览 LiteFold 源码，请进入 `source/`
- 如果你是在做主工作区治理，不要默认把 `litefold/` 这一层当成普通代码目录重构
