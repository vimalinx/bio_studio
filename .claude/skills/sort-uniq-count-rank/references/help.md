# sort-uniq-count-rank Help Reference

- Command: `sort-uniq-count-rank`
- Sources: conda_bioconda
- Local executable: `/home/vimalinx/miniforge3/envs/bio/bin/sort-uniq-count-rank`
- Summary: CLI installed by bioconda package entrez-direct.
- Package names: entrez-direct

## Captured Version

```text
$ sort-uniq-count-rank --version
sort: options '-in' are incompatible
sort: stray character in field spec: invalid field specification ‘2rsin’
```

## Captured Help

```text
$ sort-uniq-count-rank --help
sort: stray character in field spec: invalid field specification ‘2s’
```

## Captured Man Page

```text
No man page captured.
```
