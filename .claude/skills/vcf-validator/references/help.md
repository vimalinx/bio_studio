# vcf-validator Help Reference

- Command: `vcf-validator`
- Sources: conda_bioconda
- Local executable: `/home/vimalinx/miniforge3/envs/bio/bin/vcf-validator`
- Summary: CLI installed by bioconda package vcftools.
- Package names: vcftools

## Captured Version

```text
$ vcf-validator --version
Unknown parameter or non-existent file: "--version". Run -h for help.
 at /home/vimalinx/miniforge3/envs/bio/bin/vcf-validator line 23.
	main::error("Unknown parameter or non-existent file: \"--version\". Run -h f"...) called at /home/vimalinx/miniforge3/envs/bio/bin/vcf-validator line 46
	main::parse_params() called at /home/vimalinx/miniforge3/envs/bio/bin/vcf-validator line 13
```

## Captured Help

```text
$ vcf-validator --help
Usage: vcf-validator [OPTIONS] file.vcf.gz
Options:
   -d, --duplicates                 Warn about duplicate positions.
   -u, --unique-messages            Output all messages only once.
   -h, -?, --help                   This help message.
```

## Captured Man Page

```text
No man page captured.
```
