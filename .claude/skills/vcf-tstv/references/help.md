# vcf-tstv Help Reference

- Command: `vcf-tstv`
- Sources: conda_bioconda
- Local executable: `/home/vimalinx/miniforge3/envs/bio/bin/vcf-tstv`
- Summary: CLI installed by bioconda package vcftools.
- Package names: vcftools

## Captured Version

```text
$ vcf-tstv --version
Unknown parameter "--version". Run -h for help.
 at /home/vimalinx/miniforge3/envs/bio/bin/vcf-tstv line 20.
	main::error("Unknown parameter \"--version\". Run -h for help.\x{a}") called at /home/vimalinx/miniforge3/envs/bio/bin/vcf-tstv line 36
	main::parse_params() called at /home/vimalinx/miniforge3/envs/bio/bin/vcf-tstv line 10
```

## Captured Help

```text
$ vcf-tstv --help
Usage: cat file.vcf | vcf-tstv
Options:
   -h, -?, --help                  This help message.
```

## Captured Man Page

```text
No man page captured.
```
