# remove-dup Help Reference

- Command: `removeDup`
- Sources: conda_bioconda
- Local executable: `/home/vimalinx/miniforge3/envs/bio/bin/removeDup`
- Summary: CLI installed by bioconda package subread.
- Package names: subread

## Captured Version

```text
$ removeDup --version
removeDup: unrecognized option '--version'

removeDup Version 2.1.1

  Remove duplicated reads.

Usage:
  ./removeDup [options] -i <input_file> -o <output_file>

Required arguments:

  -i <string> Name of input file in SAM/BAM format.

  -o <string> Name of output SAM file including filtered reads. The format is
              BAM unless '-S' is specified.

Optional arguments:

  -S          Generate the SAM format output.

  -r <int>    Specify the duplication cutoff. All the reads mapped to a location
              are removed from the output if the number of reads mapped to this
              location is equal or higher than the cutoff. 10 by default.

  -t <string> A directory storing temporary files generated by the program.

ERROR: The input file is neither a BAM file nor a SAM file.
```

## Captured Help

```text
$ removeDup --help
removeDup: unrecognized option '--help'

removeDup Version 2.1.1

  Remove duplicated reads.

Usage:
  ./removeDup [options] -i <input_file> -o <output_file>

Required arguments:

  -i <string> Name of input file in SAM/BAM format.

  -o <string> Name of output SAM file including filtered reads. The format is
              BAM unless '-S' is specified.

Optional arguments:

  -S          Generate the SAM format output.

  -r <int>    Specify the duplication cutoff. All the reads mapped to a location
              are removed from the output if the number of reads mapped to this
              location is equal or higher than the cutoff. 10 by default.

  -t <string> A directory storing temporary files generated by the program.

ERROR: The input file is neither a BAM file nor a SAM file.
```

## Captured Man Page

```text
No man page captured.
```
