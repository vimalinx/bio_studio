# zoom2sam-pl Help Reference

- Command: `zoom2sam.pl`
- Sources: conda_bioconda
- Local executable: `/home/vimalinx/miniforge3/envs/bio/bin/zoom2sam.pl`
- Summary: CLI installed by bioconda package samtools.
- Package names: samtools

## Captured Version

```text
$ zoom2sam.pl --version
/home/vimalinx/miniforge3/envs/bio/bin/zoom2sam.pl version [unknown] calling Getopt::Std::getopts (version 1.12 [paranoid]),
running under Perl version 5.32.1.
  [Now continuing due to backward compatibility and excessive paranoia.
   See 'perldoc Getopt::Std' about $Getopt::Std::STANDARD_HELP_VERSION.]
Usage: zoom2sam.pl [-p] <readLen> <aln.zoom>
Warnings: This script only supports the default Illumina outputs.
```

## Captured Help

```text
$ zoom2sam.pl --help
/home/vimalinx/miniforge3/envs/bio/bin/zoom2sam.pl version [unknown] calling Getopt::Std::getopts (version 1.12 [paranoid]),
running under Perl version 5.32.1.

Usage: zoom2sam.pl [-OPTIONS [-MORE_OPTIONS]] [--] [PROGRAM_ARG1 ...]

The following single-character options are accepted:
	Boolean (without arguments): -p

Options may be merged together.  -- stops processing of options.
  [Now continuing due to backward compatibility and excessive paranoia.
   See 'perldoc Getopt::Std' about $Getopt::Std::STANDARD_HELP_VERSION.]
Usage: zoom2sam.pl [-p] <readLen> <aln.zoom>
Warnings: This script only supports the default Illumina outputs.
```

## Captured Man Page

```text
No man page captured.
```
