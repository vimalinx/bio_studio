# esl-compalign Help Reference

- Command: `esl-compalign`
- Sources: conda_bioconda
- Local executable: `/home/vimalinx/miniforge3/envs/bio/bin/esl-compalign`
- Summary: CLI installed by bioconda package hmmer.
- Package names: hmmer

## Captured Version

```text
$ esl-compalign --version
Failed to parse command line: No such option "--version".
Usage: esl-compalign [-options] <trusted file> <test file>
  Both files must be in Stockholm format with #=GC RF markup.
  Sequences must occur in the same order in the two files.
  Number of non-gap characters in #=GC RF markup must be identical.
  Note: accuracy is computed differently than in Squid's compalign.
  See the manual page for details on how accuracy is computed.

To see more help on available options, do esl-compalign -h
```

## Captured Help

```text
$ esl-compalign --help
Failed to parse command line: No such option "--help".
Usage: esl-compalign [-options] <trusted file> <test file>
  Both files must be in Stockholm format with #=GC RF markup.
  Sequences must occur in the same order in the two files.
  Number of non-gap characters in #=GC RF markup must be identical.
  Note: accuracy is computed differently than in Squid's compalign.
  See the manual page for details on how accuracy is computed.

To see more help on available options, do esl-compalign -h
```

## Captured Man Page

```text
No man page captured.
```
